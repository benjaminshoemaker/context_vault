{
  "name": "ai-context",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -r \"pnpm -C services/vault-api dev\" \"pnpm -C services/mcp-vault dev\"",
    "test": "vitest --run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "typescript": "^5.6.3",
    "tsx": "^4.19.2",
    "@types/node": "^22.5.4",
    "vitest": "^2.0.5",
    "supertest": "^7.0.0",
    "@types/supertest": "^2.0.16",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
